# Fix lỗi "Stripping types is currently unsupported for files under node_modules"
# Node.js 22+ có experimental TypeScript stripping, nhưng không hỗ trợ cho node_modules
# Tắt tính năng này để EAS CLI và Expo config có thể hoạt động đúng
node-options=--no-experimental-strip-types
