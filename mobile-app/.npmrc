# Tắt TypeScript stripping của Node.js 22+ cho node_modules
# Điều này ngăn lỗi "Unknown file extension .ts" khi EAS/Expo cố load các package
node-options=--no-experimental-strip-types

# Đảm bảo npm không strict về peer dependencies
legacy-peer-deps=true
